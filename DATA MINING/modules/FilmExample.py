#If you don't get what is going on check the class notes

#imports
import numpy as np

#initializations
A = np.array([5, 1, 3])
M = np.array([4, 5, 1])

#multiplying using dot product method
mul = np.dot(A, M)

#displaying the multiplication
print(mul)